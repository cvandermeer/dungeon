<%= form_for @monster, remote: true do |f| %>
  <%= f.text_field :name, placeholder: 'naam' %>
  <%= f.number_field :level, placeholder: 'strenght' %>
   <%= f.number_field :treasure, placeholder: 'treasure' %>
  <%= f.submit 'Oproepen', data: { disable_with: 'Aan het versturen...' } %>
<% end %>